{
  "name": "commodities-gold-silver",
  "install": "cd /agent/repos/commodities-gold-silver && python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements.txt"
}
